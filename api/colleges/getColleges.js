import { useRuntimeConfig } from '#imports';

export default async function getColleges(opts, id) {
  const config = useRuntimeConfig();

  try {
    const headers = {
      Accept: 'application/vnd.api+json',
    };

    const { data } = await useAsyncData(`colleges-${id}`, () =>
      $fetch(`v1/organization/colleges`, {
        baseURL: config.public.baseURL,
        headers,
        method: 'GET',
        params: {
          ...opts,
        },
      }),
    );

    return data.value;
  } catch {
    return [];
  }
}
