import { useRuntimeConfig } from '#imports';

export default async function getSubjectsRelationships(id, opts) {
  const config = useRuntimeConfig();

  try {
    const headers = {
      Accept: 'application/vnd.api+json',
    };

    const { data } = await useAsyncData('subjects-relationships', () =>
      $fetch(`v1/address/districts/${id}/relationships/subjects`, {
        baseURL: config.public.baseURL,
        headers,
        method: 'GET',
        params: {
          ...opts,
        },
      }),
    );

    return data.value;
  } catch {
    return [];
  }
}
