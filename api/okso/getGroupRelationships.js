import { useRuntimeConfig } from '#imports';

export default async function getGroupRelationships(id, opts) {
  const config = useRuntimeConfig();

  try {
    const headers = {
      Accept: 'application/vnd.api+json',
    };

    const { data } = await useAsyncData('group-relationships', () =>
      $fetch(`v1/education/okso-specialties/${id}/relationships/group`, {
        baseURL: config.public.baseURL,
        headers,
        method: 'GET',
        params: {
          ...opts,
        },
      }),
    );

    return data.value;
  } catch {
    return [];
  }
}
